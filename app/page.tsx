import CalculatorCard from "@/components/CalculatorCard";

export default function Home() {
		// <main className="flex flex-row w-full h-screen overflow-hidden items-center justify-center bg-slate-950">
	return (
		<main className="fixed top-0 left-0 right-0 bottom-0 flex items-center justify-center bg-slate-950">
			<CalculatorCard/>
		</main>
	);
}
